# Use Remote server as Local MCP Proxy Server in claude desktop app
from fastmcp import FastMCP

mcp = FastMCP.as_proxy(
    "https://my-dairy-server.fastmcp.app/mcp",
    name = "My Dairy Proxy Server"
)

if __name__ == "__main__":
    mcp.run(transport="streamable-http")